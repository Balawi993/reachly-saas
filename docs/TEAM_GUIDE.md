# 👥 دليل الفريق - Reachly Project

**للفريق الجديد والمطورين**

---

## 🎯 ما هو المشروع؟

**Reachly** هو منصة أتمتة التواصل على Twitter تسمح للمستخدمين بإرسال رسائل مباشرة ومتابعة حسابات بشكل تلقائي وآمن.

### الهدف الأساسي:
- **أتمتة الرسائل المباشرة** على Twitter
- **أتمتة المتابعة** للحسابات المستهدفة  
- **إدارة حملات متعددة** من لوحة واحدة
- **حماية من الحظر** عبر نظام Pacing محكم

---

## 🏗️ البنية التقنية (للمطورين)

### Frontend (React)
```
src/
├── pages/           # الصفحات الرئيسية
├── components/      # المكونات القابلة للإعادة
├── lib/            # المكتبات والـ API
└── hooks/          # React Hooks مخصصة
```

### Backend (Node.js)
```
server/
├── index.ts        # الخادم الرئيسي
├── campaign-runner.ts  # محرك الحملات
├── twitter.ts      # تكامل Twitter
└── db-postgres.ts  # قاعدة البيانات
```

### التقنيات المستخدمة:
- **Frontend:** React 18 + TypeScript + TailwindCSS
- **Backend:** Node.js + Express + TypeScript
- **Database:** PostgreSQL + Redis
- **Deployment:** Railway
- **Authentication:** JWT + bcrypt

---

## 📱 الصفحات والوظائف

### 1. 🏠 Dashboard (الرئيسية)
**المسار:** `/dashboard`

**ما تعرضه:**
- إحصائيات سريعة (الرسائل، الحملات، الحسابات)
- آخر 5 حملات نشطة
- إجراءات سريعة (إنشاء حملة، إدارة حسابات)

**للمطورين:**
- تحديث تلقائي كل 5 ثوانٍ
- استخدام React hooks للبيانات المباشرة
- مكونات قابلة للإعادة للإحصائيات

### 2. 📧 Campaigns (حملات الرسائل)
**المسار:** `/campaigns`

**ما تعرضه:**
- قائمة جميع حملات الرسائل المباشرة
- حالة كل حملة (نشطة، متوقفة، مكتملة)
- أزرار التحكم (تشغيل، إيقاف، حذف)
- إحصائيات مفصلة لكل حملة

**الميزات التقنية:**
- تحديث مباشر للحالة
- نظام فلترة وبحث
- تصدير البيانات CSV
- إدارة الحالة المعقدة

### 3. 🎯 Campaign Detail (تفاصيل الحملة)
**المسار:** `/campaigns/:id`

**التبويبات:**
1. **Overview:** الإحصائيات والتقدم العام
2. **Targets:** قائمة الأهداف مع بحث وفلترة
3. **Settings:** إعدادات الحملة والرسالة

**للمطورين:**
- استخدام Tabs من shadcn/ui
- نظام بحث وفلترة متقدم
- عرض البيانات في جداول قابلة للتصفح

### 4. 🧙‍♂️ Campaign Wizard (معالج الإنشاء)
**المسار:** `/campaigns/new`

**الخطوات:**
1. **المعلومات الأساسية:** اسم، حساب، علامات
2. **الجمهور:** اختيار الأهداف (يدوي أو استخراج)
3. **الرسالة:** كتابة قالب مع متغيرات
4. **الإعدادات:** سرعة الإرسال والحدود
5. **المراجعة:** مراجعة نهائية

**الميزات التقنية:**
- Multi-step form مع حفظ تلقائي
- اختصارات لوحة المفاتيح
- خطوات قابلة للنقر والتنقل
- معاينة مباشرة للرسالة

### 5. 👥 Follow Campaigns (حملات المتابعة)
**المسار:** `/follow-campaigns`

**الوظيفة:**
- إنشاء حملات لمتابعة حسابات تلقائياً
- نفس نظام الـ Pacing للحماية من الحظر
- إحصائيات المتابعة الناجحة/الفاشلة

### 6. 🔗 Accounts (إدارة الحسابات)
**المسار:** `/accounts`

**الوظيفة:**
- إضافة حسابات Twitter متعددة
- عرض حالة كل حساب (نشط/خطأ)
- إحصائيات الاستخدام لكل حساب
- حذف وتحديث الحسابات

**التصميم:**
- عرض شبكي (Grid) للحسابات
- بطاقات تفاعلية مع hover effects
- مؤشرات حالة ملونة
- قوائم إجراءات منسدلة

### 7. ⚙️ Settings (الإعدادات)
**المسار:** `/settings`

**الأقسام:**
- الملف الشخصي
- الأمان وكلمة المرور
- إعدادات الإشعارات
- التفضيلات العامة

---

## ⚙️ النظم الأساسية (للفهم)

### 1. نظام Pacing & Retry (الحماية من الحظر)

**المشكلة:** إرسال رسائل كثيرة بسرعة = حظر الحساب

**الحل:**
```javascript
const pacing = {
  perMinute: 3,        // 3 رسائل فقط في الدقيقة
  delayMin: 15,        // انتظار 15 ثانية على الأقل
  delayMax: 30,        // انتظار 30 ثانية كحد أقصى
  dailyCap: 50,        // 50 رسالة يومياً فقط
  retryAttempts: 2     // محاولتان إضافيتان عند الفشل
}
```

**كيف يعمل:**
1. إرسال رسالة
2. انتظار عشوائي (15-30 ثانية)
3. فحص الحدود اليومية
4. إرسال الرسالة التالية
5. عند الفشل: إعادة المحاولة بعد تأخير

### 2. نظام الأمان

**تشفير البيانات:**
- كوكيز Twitter مشفرة بـ AES-256
- كلمات المرور مشفرة بـ bcrypt
- الجلسات محمية بـ JWT

**حماية من الهجمات:**
- Rate limiting للطلبات
- Input validation لجميع البيانات
- HTTPS إجباري

### 3. نظام قاعدة البيانات

**الجداول الرئيسية:**
- `users` - المستخدمين
- `accounts` - حسابات Twitter
- `campaigns` - حملات الرسائل
- `targets` - الأهداف المستهدفة
- `follow_campaigns` - حملات المتابعة

---

## 🔄 سير العمل (للفهم العام)

### إنشاء حملة جديدة:
1. المستخدم يدخل معالج الإنشاء
2. يختار الحساب والإعدادات
3. يضيف قائمة الأهداف
4. يكتب قالب الرسالة
5. يحدد إعدادات السرعة والأمان
6. يحفظ الحملة

### تشغيل الحملة:
1. المستخدم يضغط "تشغيل"
2. النظام يبدأ معالجة الأهداف
3. لكل هدف: فحص الحدود → إرسال → انتظار
4. تحديث الإحصائيات في الوقت الفعلي
5. إيقاف تلقائي عند انتهاء الأهداف

---

## 🛠️ للمطورين الجدد

### إعداد البيئة:
```bash
# 1. تثبيت المتطلبات
npm install

# 2. إعداد قاعدة البيانات
npm run setup-db

# 3. تشغيل التطوير
npm run dev:all
```

### هيكل الكود:
- **Components:** مكونات قابلة للإعادة
- **Pages:** صفحات التطبيق
- **Hooks:** منطق مشترك
- **API:** تكامل مع الخادم
- **Utils:** وظائف مساعدة

### أهم الملفات:
- `src/lib/api.ts` - عميل API
- `server/campaign-runner.ts` - محرك الحملات
- `server/twitter.ts` - تكامل Twitter
- `src/pages/Dashboard.tsx` - الصفحة الرئيسية

---

## 📊 الإحصائيات والبيانات

### ما نتتبعه:
- **عدد الرسائل المرسلة** لكل حملة
- **معدل النجاح** (نسبة الرسائل الناجحة)
- **الأخطاء وأسبابها** لكل محاولة
- **استخدام كل حساب** (عدد الحملات، الرسائل)
- **الأداء العام** للنظام

### كيف نعرضها:
- **Dashboard:** إحصائيات سريعة
- **Campaign Detail:** تفاصيل كل حملة
- **Accounts:** إحصائيات كل حساب
- **Real-time updates:** تحديث مباشر

---

## 🚀 النشر والاستضافة

### Railway Deployment:
- **Database:** PostgreSQL تلقائي
- **Cache:** Redis للطوابير
- **Environment:** متغيرات البيئة آمنة
- **Auto-deploy:** نشر تلقائي من GitHub

### متغيرات البيئة المطلوبة:
```env
JWT_SECRET=your-secret-key
COOKIE_ENCRYPTION_KEY=your-encryption-key
DATABASE_URL=postgresql://...
REDIS_URL=redis://...
```

---

## 🎯 نصائح للفريق

### للمصممين:
- التصميم يستخدم **TailwindCSS**
- المكونات من **shadcn/ui**
- الألوان والثيم في `globals.css`
- الأيقونات من **Lucide React**

### للمطورين:
- الكود مكتوب بـ **TypeScript**
- استخدام **React Hooks** للحالة
- **API client** موحد في `lib/api.ts`
- **Error handling** شامل في كل مكان

### لمديري المشاريع:
- الميزات منظمة في **صفحات منفصلة**
- كل صفحة لها **وظيفة واضحة**
- **التحديثات مباشرة** بدون إعادة تحميل
- **UX متسق** عبر التطبيق

### للاختبار:
- اختبار **إنشاء حملة** كاملة
- اختبار **إضافة حسابات** جديدة
- اختبار **تشغيل وإيقاف** الحملات
- مراقبة **الأداء والأخطاء**

---

## 📞 الدعم والمساعدة

### الموارد:
- **README.md** - دليل البدء
- **docs/PROJECT_DOCUMENTATION.md** - التوثيق الشامل
- **docs/API_DOCS.md** - توثيق API
- **docs/TROUBLESHOOTING.md** - حل المشاكل

### للأسئلة:
- **التقنية:** GitHub Issues
- **التصميم:** مراجعة الكود
- **الوظائف:** اختبار مباشر
- **الأداء:** مراقبة السجلات

---

**مرحباً بكم في فريق Reachly! 🚀**

**هدفنا:** بناء أفضل منصة أتمتة تواصل آمنة وفعالة على Twitter.
