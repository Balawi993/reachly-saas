# 🔧 حل المشاكل - Troubleshooting

## ❌ المشكلة: الحساب يظهر "Invalid"

### الأسباب المحتملة:

#### 1. الكوكيز منتهية الصلاحية
**الحل:**
- سجل خروج من Twitter
- سجل دخول مرة أخرى
- احصل على كوكيز جديدة

#### 2. اسم المستخدم خاطئ
**الحل:**
- تأكد من إدخال username بدون @
- مثال: `oblawj` وليس `@oblawj`

#### 3. الحساب محمي أو معلق
**الحل:**
- تأكد من أن الحساب نشط
- تأكد من أن الحساب ليس محمياً (Private)

#### 4. Twitter API لا يستجيب
**الحل:**
- انتظر قليلاً وحاول مرة أخرى
- تحقق من اتصال الإنترنت

---

## ❌ المشكلة: "Last validated: Invalid Date"

**السبب:** مشكلة في عرض التاريخ

**الحل:** تم إصلاحها في آخر تحديث. أعد تحميل الصفحة.

---

## ❌ المشكلة: "User not found"

### عند إضافة حساب:
**السبب:** اسم المستخدم غير موجود أو خاطئ

**الحل:**
1. تحقق من اسم المستخدم على Twitter
2. أدخل username بدون @
3. تأكد من عدم وجود مسافات

### عند إرسال رسائل:
**السبب:** الهدف غير موجود أو حذف حسابه

**الحل:**
1. تحقق من أن الحساب المستهدف موجود
2. تحقق من كتابة الاسم بشكل صحيح

---

## 🔍 كيف تتحقق من الكوكيز؟

### الخطوات:

1. **افتح Twitter في المتصفح**
2. **سجل دخول**
3. **اضغط F12**
4. **اذهب إلى Application → Cookies → x.com**
5. **تحقق من وجود:**
   - `auth_token` ✓
   - `ct0` ✓

### الصيغة الصحيحة:

```json
[
  {
    "name": "auth_token",
    "value": "القيمة_هنا"
  },
  {
    "name": "ct0",
    "value": "القيمة_هنا"
  }
]
```

أو:

```json
{
  "auth_token": "القيمة_هنا",
  "ct0": "القيمة_هنا"
}
```

---

## 🐛 كيف تتحقق من الأخطاء؟

### في Backend (Terminal):

راقب الـ console في terminal حيث يعمل `npm run server`:

```
✓ Account validated successfully  ← جيد
✗ Twitter API error: ...          ← خطأ
```

### في Frontend (Browser):

افتح Developer Tools (F12) → Console:

```
Error: Invalid credentials  ← خطأ
Account added successfully  ← جيد
```

---

## 📝 خطوات التشخيص

### 1. تحقق من الكوكيز
```bash
# في terminal
node test-cookies.js
```

يجب أن ترى:
```
auth_token: ...
ct0: ...
Both present? true
```

### 2. تحقق من Backend
```bash
# تأكد من تشغيل Backend
npm run server
```

يجب أن ترى:
```
🚀 Server running on http://localhost:3001
```

### 3. تحقق من Frontend
```bash
# تأكد من تشغيل Frontend
npm run dev
```

يجب أن ترى:
```
➜  Local:   http://localhost:8080/
```

### 4. جرب إضافة الحساب مرة أخرى
- احذف الحساب القديم
- احصل على كوكيز جديدة
- أضف الحساب مرة أخرى

---

## ⚠️ ملاحظات مهمة

### الكوكيز تنتهي صلاحيتها
- صلاحية الكوكيز: 7-30 يوم
- إذا سجلت خروج من Twitter، الكوكيز تصبح غير صالحة
- إذا غيرت كلمة المرور، الكوكيز تصبح غير صالحة

### Twitter قد يحظر الطلبات
- إذا أرسلت طلبات كثيرة جداً
- إذا كان نشاطك غير طبيعي
- الحل: انتظر قليلاً وحاول مرة أخرى

### استخدم حساب اختبار
- لا تستخدم حسابك الرئيسي
- أنشئ حساب جديد للاختبار
- هذا أكثر أماناً

---

## 🆘 إذا لم تحل المشكلة

### 1. احذف قاعدة البيانات وابدأ من جديد
```bash
# أوقف التطبيق (Ctrl+C)
rm reachly.db
npm run dev:all
```

### 2. تحقق من console logs
- راقب terminal للأخطاء
- راقب Browser Console للأخطاء

### 3. تحقق من الكوكيز مرة أخرى
- احصل على كوكيز جديدة
- تأكد من الصيغة الصحيحة

### 4. جرب حساب آخر
- جرب حساب Twitter مختلف
- تأكد من أن الحساب نشط

---

## ✅ الحل السريع

إذا كنت في عجلة:

```bash
# 1. أوقف التطبيق
Ctrl+C

# 2. احذف قاعدة البيانات
rm reachly.db

# 3. شغل التطبيق
npm run dev:all

# 4. سجل دخول مرة أخرى

# 5. احصل على كوكيز جديدة من Twitter

# 6. أضف الحساب مرة أخرى
```

---

**آخر تحديث**: 2025-01-10
